import { Typography } from '@mui/material'

export default function AppointmentsPage() {
  return (
    <div>
      <Typography variant="h4">Sistema de Citas</Typography>
      <Typography>Aquí implementaremos el calendario de citas</Typography>
    </div>
  )
}